# Block dependency install scripts (preinstall/install/postinstall) to mitigate
# supply-chain malware in the npm ecosystem (ref: issue #2). This is a pure-JS
# browser build — no dependency requires native install-time compilation — so
# ignoring scripts does not affect the build.
ignore-scripts=true
